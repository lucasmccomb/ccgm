# Disposition of the external plan review

Updated September 5, 2026. This records design decisions, not another agent's approval of the revised plan. The user subsequently chose a smaller first delivery and an upfront one-to-three review count, default one, for both X-Plan and X-Plan A.

- F1 — Adopt the smaller first delivery. Limit it to two reviewable changes: integration and pilot workflow policy. Preserve both host directions and put broader CCGM runtime integration in a separate follow-on roadmap. The original request did include broader workspace integration, so it is deferred rather than treated as unsolicited scope.
- F2 — Adopt structured evidence-based disagreement and frozen-artifact review; preserve the requested mutual resolution. The cited research improves a convergent protocol rather than rejecting convergence categorically. Missing-goal and unresolved-resource states remain explicit; neither agent can manufacture consensus or close a supported objection through authority alone.
- F3 — Remove speculative token pools and atomic reservations. Use saved invocation counts, deadlines, a default three-fix-round checkpoint, and explicit evidence-supported continuation within the same overall allowance. Measure actual usage and available quota signals. Reconcile advisor-mode text explicitly.
- F4 — Evaluate the official Codex plugin first, with one chosen transport per direction. Keep only the small CCGM policy/result adapter that remains necessary. Correct the reverse-MCP claim: `claude mcp serve` exposes tools; an actual Claude reasoning agent still needs an agent invocation.
- F5 — Record both assessment and upstream anchors. Require a fresh isolated implementation baseline including advisor-mode. Put maintained plan sources in a versionable docs directory; leave machine-specific audit artifacts ignored. New issue IDs are assigned at execution, not fabricated here.
- F6 — Superseded by the user's explicit choice: ask for one, two, or three adversarial passes before both planning variants start, default one. Preserve opposite/origin/opposite alternation for the chosen prefix. All passes retain whole-plan coverage; the last selected pass is final. The cited same-model paper does not establish which cross-provider pass has the least value.
- F7 — Pilot X-Plan/X-Plana, adrev, and ETP, with only necessary shared compatibility changes. Measure useful findings, false positives, seeded-defect misses, resolution, latency, and usage before migrating the remaining sites. Independent overlap is descriptive, not proof of validity.
- F8 — Specify native authentication boundaries and verify effective read-only/tool restrictions for each provider. Follow the official terms applicable to the selected account/use case; do not present third-party anecdotes as blanket permission for hosted or credential-sharing products.
- F9 — Make headless artifact checks and dedicated-runner live testing explicit. Any later visible app/input/device testing requires the recorded runner grant. The user-requested HTML remains a local document preview.
- F10 — Add bounded work units, owner/reviewer roles, dependencies, acceptance and must-fail cases, deterministic CI gates, live smoke prerequisites, and the follow-up-completion contract. Assign real issues and agents when execution starts. Distinguish existing passing baseline checks from implementation validation.
- F11 — Validate final structured payloads locally, including exit-zero malformed-output fixtures. The cited issue was reported against Codex CLI 0.116.0 and is closed; it is a useful regression case, not evidence that the currently installed version necessarily has the defect.

Supporting primary sources: [official plugin](https://github.com/openai/codex-plugin-cc), [Adversarial Review paper](https://arxiv.org/html/2608.18167), [Claude MCP tools boundary](https://code.claude.com/docs/en/mcp#use-claude-code-as-an-mcp-server), [authentication and usage](https://code.claude.com/docs/en/legal-and-compliance), and [structured-output issue](https://github.com/openai/codex/issues/15451).
