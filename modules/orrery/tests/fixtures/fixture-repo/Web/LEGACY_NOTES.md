# Web (fixture, legacy)
Uppercase directory name exercising the area-id sanitization rule (Web -> web,
colliding with web/ -> collision suffix). Filenames here are deliberately
disjoint from web/ so case-insensitive checkouts never collide on a path.
