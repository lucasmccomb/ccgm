# acme-shop example environment (all values fictional)
API_BASE_URL=https://api.acme-shop.example
PUBLIC_ASSET_HOST=https://cdn.acme-shop.example
# POSITIVE screening case: a real-shaped (but fake) GitHub token the
# SECRET_PATTERNS class matches and must quarantine.
GH_TOKEN=ghp_0bAdC0ffeeDeadBeef0123456789abcdefZZ
# NEGATIVE screening case: a documented Stripe TEST key shape that must NOT be
# quarantined (sk_test_ does not match the sk- class).
STRIPE_KEY=sk_test_fixturefake123
